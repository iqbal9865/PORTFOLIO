import React from 'react';
import Navbar from '../Shared/Navbar/Navbar';

const CoverLetter = () => {
    return (
        <div>
            <Navbar/>
        </div>
    );
};

export default CoverLetter;